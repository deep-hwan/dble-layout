import React, { ComponentPropsWithoutRef } from "react";
import { TxtElementType, TxtPropsType } from "../types";
declare const Txt: React.MemoExoticComponent<(<T extends TxtElementType = "p">({ as, children, className, txtSize, txtWeight, txtAlign, txtColor, txtShadow, txtTransform, txtDecoration, lineHeight, whiteSpace, ellipsis, sizes, position, padding, margin, borderRadius, border, background, opacity, gradient, shadow, axis, scale, rotate, zIndex, cursor, userSelect, transition, _hover, _focus, _active, mq, css: cssProp, ...rest }: TxtPropsType<T> & React.PropsWithoutRef<React.ComponentProps<T>>) => import("@emotion/react/jsx-runtime").JSX.Element)>;
export { Txt };
