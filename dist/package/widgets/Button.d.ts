import React, { ComponentPropsWithoutRef } from "react";
import { ButtonPropsType } from "../types";
declare const Button: React.MemoExoticComponent<({ children, className, txtSize, txtWeight, txtAlign, txtColor, txtShadow, txtTransform, txtDecoration, lineHeight, whiteSpace, ellipsis, display, sizes, flex, position, padding, margin, borderRadius, border, backgroundFill, opacity, gradient, shadow, axis, scale, rotate, zIndex, cursor, userSelect, transition, _hover, _focus, _active, _disabled, mq, css: cssProp, ...rest }: ButtonPropsType & ComponentPropsWithoutRef<"button">) => import("@emotion/react/jsx-runtime").JSX.Element>;
export { Button };
